#Work-Bill-Generator
When Chromebooks aren't returned we need to be able to generate a bill based on a csv instead of hand typing in all the information as we currently do.
Using AI to speed up coding.

#TODO
Need to add second csv functionality because currently our bills area doesn't have PO or PROG ID - pull in INV
And the date should reflect the date the bill was generated
And currently we look up addresses manually so eventually a 3rd csv to pull address from IF
And also we currently create this csv but if I could pull it from returns/replacements that would work better.
Would use ID as primary key for returns/replacements/not returned/ to match with IF and use State Tag from returns/replacements/not returned/ to match with INV


Portions of this code were generated with assistance from Claude (Anthropic)
